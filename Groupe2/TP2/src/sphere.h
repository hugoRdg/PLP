#ifndef SPHERE_H
#define SPHERE_H

// Fonction pour calculer le volume d'une sphère
float calculer_volume(float rayon);

// Fonction pour calculer la surface d'une sphère
float calculer_surface(float rayon);

#endif

/*
    Ce fichier est l'en-tête qui déclare les prototypes des fonctions utilisées
    pour calculer le volume et la surface d'une sphère.
    
    Il est inclus dans plusieurs fichiers source afin d'assurer que les fonctions
    sont correctement définies et accessibles dans tous les fichiers.
*/